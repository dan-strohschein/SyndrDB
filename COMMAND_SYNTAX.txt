CREATE DATABASE "<DATABASE_NAME>";

DELETE DATABASE "<DATABASE_NAME>";

SELECT DATABASES "<DATABASE_NAME>";

USE "<DATABASE_NAME>";

CREATE BUNDLE "<BUNDLE_NAME>" WITH FIELDS ({"<FIELDNAME>", <FIELDTYPE>, <REQUIRED>, <UNIQUE>}, {"<FIELDNAME>", <FIELDTYPE>, <REQUIRED>, <UNIQUE>});

DELETE BUNDLE "<BUNDLE_NAME>";

UPDATE BUNDLE "<BUNDLE_NAME>" [update_operations];

ADD DOCUMENT TO BUNDLE "<BUNDLE_NAME>" WITH ({key=value, key=value});

SELECT DOCUMENTS FROM "<BUNDLE_NAME>";

SELECT DOCUMENTS FROM "<BUNDLE_NAME>" WHERE <CONDITIONS>;

SELECT * FROM "<BUNDLE_NAME>";

SELECT DOCUMENTS FROM "<BUNDLE_NAME>" JOIN "<OTHER_BUNDLE>" ON <JOIN_CONDITIONS> WHERE <CONDITIONS>;

SELECT DOCUMENTS FROM "<BUNDLE_NAME>" WHERE <CONDITIONS> ORDER BY <FIELD_NAME> [ASC|DESC];

SELECT DOCUMENTS FROM "<BUNDLE_NAME>" WHERE <CONDITIONS> GROUP BY <FIELD_NAME>;

UPDATE DOCUMENTS IN BUNDLE "<BUNDLE_NAME>" ({key=new_value, key=new_value}) WHERE <CONDITIONS>;

DELETE DOCUMENTS FROM BUNDLE "<BUNDLE_NAME>" WHERE <CONDITIONS>;

CREATE BTREE INDEX "<INDEX_NAME>" ON BUNDLE "<BUNDLE_NAME>" (<FIELD_NAME>, <FIELD_NAME>);

CREATE HASH INDEX "<INDEX_NAME>" ON BUNDLE "<BUNDLE_NAME>" (<FIELD_NAME>);

SHOW DATABASES;

SHOW BUNDLES;

SHOW BUNDLES FOR "<DATABASE_NAME>";

SHOW BUNDLE "<BUNDLE_NAME>";

SHOW USERS;

SHOW RATE LIMIT;

GRANT <PERMISSION_TYPE> ON <RESOURCE> TO <USER_OR_ROLE>;

ATTACH <RESOURCE_SPECIFICATION>;

INVALIDATE SESSION;

INVALIDATE SESSION "<SESSION_ID>";